{
  "name": "capital-allocator",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Reads user-supplied URLs, extracts investable candidates, and runs a quantitative investment-committee process to recommend where to put money.",
  "engines": { "node": ">=18" },
  "scripts": {
    "start": "node server/index.js",
    "dev": "node --watch server/index.js",
    "test": "node --test test/*.test.js"
  }
}
